한국어 | [English](PHASE1_PROBE_GUIDE.en.md)

# PHASE1_PROBE_GUIDE.md — probe 선정과 K등급 판정

이 파일은 **정책(policy)** 이다. `CLAUDE.md` Phase 1을 세분한 것이며, 모든
프로젝트가 공유한다. 진행 상태는 절대 여기에 기록하지 않는다 — 상태는 각
프로젝트의 `background.md`에 있다.

**`CLAUDE.md`가 이 파일에 위임한 범위** (노드 선정, probe 채점, 등급 전파,
진단표의 열 구성) **안에서는 이 파일이 정본이다.** 그 밖에서 충돌하면
`CLAUDE.md`가 우선한다.

여기서 **정하지 않는 것** (전부 `CLAUDE.md` Phase 1에 있고, 여기서 반복하지
않는다): 지식 척도 K0–K3의 정의, 질문 예산(턴당 문항 수·총 턴 수), probe의
언어와 조판 제약, baseline 확정 절차, 깊이 결정표(사용 방식 × K등급 → L).

여기서 **정하는 것**: 어느 노드를 물을 것인가, 답을 어떻게 등급으로 옮길
것인가, 묻지 않은 노드의 등급을 그래프에서 어떻게 채울 것인가.

---

## 1. 두 원칙

**질문은 커리큘럼을 바꿀 때만 한다.** 진단은 시험이 아니다. 어떤 답이 나와도
확정 깊이가 같은 노드는 묻지 않는다. 질문 하나의 가치는 그 답이 L을 바꾸는지로
잰다.

> 관련 없는 노드를 정밀하게 재는 것보다, 커리큘럼이 걸린 불확실성 하나를 푸는
> 데 질문을 쓰는 편이 낫다.

높은 등급에는 증거가 필요하다. 그러나 **낮게 매기는 것도 공짜가 아니다.**
등급을 낮추면 안전한 쪽으로 기운다는 생각은 틀렸다. K를 낮추면 (a) 이미 아는
내용을 다시 쓰게 되고 (`CLAUDE.md` §1 목표와 금지 사항에 정면으로 어긋난다),
(b) 그만큼의 지면이 정말 써야 할 L4 항목에서 빠져나간다.

---

## 2. K는 세 개다. 섞지 않는다

| 이름 | 뜻 | 출처 |
|---|---|---|
| **직접 증거** | probe 응답, 또는 사용자의 등급 자기지정 | 질문 |
| **그래프 추정** | 간선이 허용하는 상한 (§3) | 전파 |
| **확정 K** | Phase 2의 깊이 결정과 Phase 4의 재설명 금지 판정이 실제로 쓰는 값 | 아래 |

확정 규칙:

1. **직접 증거가 있으면 그것이 확정값이다.** 그래프 추정으로 깎지 않는다.
2. 직접 증거가 없고 그래프가 상한을 주면 그 값을 쓴다. 근거 열에 `상속`.
3. 둘 다 없으면 인접 노드에서 보수적으로 매긴다. 근거 열에 `추정`. Phase 2에서
   조정할 수 있다.
4. 직접 증거와 상한이 어긋나면 §4.

**측정값과 추정값을 한 칸에 뭉개지 않는다.** `background.md`의 K열은 Phase 4의
「K2 이상 재설명 금지」 판정에도 쓰인다. 증거가 K2인 노드를 그래프 사정으로
K1이라 적어 두면, 그 파일은 사용자가 아는 것을 다시 설명해도 좋다고 말하는
문서가 된다. 2번과 3번을 근거 열에서 구분하는 이유도 같다 — 상한에서 온 값은
그 상한이 바뀌면 따라 바뀌지만, 보수적 기본값은 아무 근거도 물려받지 않았다.

---

## 3. 간선을 따라 전파되는 것

**방향의 어휘를 여기서 한 번 고정한다.** 간선 「A를 이해하려면 B가 필요」에서
B를 A의 **선행**, A를 B의 **의존**이라 부른다. 이 파일도 `CLAUDE.md`도
`background.md`도 이 두 낱말만 쓴다 — 「상위/하위」는 어느 쪽이 근본이고 어느
쪽이 고급인지에 따라 정반대로 읽히므로 쓰지 않는다.

그 한 문장에는 성질이 다른 둘이 섞여 있고, 전파되는 양이 다르다.
**Phase 0에서 간선에 종류를 적는다** (`CLAUDE.md` §0-3).

| 간선 | 뜻 | 예 |
|---|---|---|
| **정의 간선** | 선행이 의존 노드의 *진술 안에* 나온다. 그것 없이 정확히 진술할 수 없다 | 교차형식 → Zariski 분해 |
| **증명 간선** | 선행이 의존 노드의 *증명에만* 쓰인다. 진술은 그것 없이 읽힌다 | Serre 쌍대성 → Kodaira 소멸정리 |

전파에는 방향이 반대인 둘이 있다. **섞지 않는다.**

### 3-1. 상한의 전파 (선행이 낮을 때, 의존 쪽으로)

- **정의 간선:** 선행이 K1 이하이면 그 의존 노드를 K2 이상으로 매기지 않는다
  (의존 노드에 직접 증거가 없는 한).
- **증명 간선:** K3만 제약한다. 의존 노드가 K3이려면 선행이 K2 이상이어야 한다.
  K2 이하에는 제약이 없다 — 증명을 모르면서 정리를 정확히 진술하고 적용하는
  것은 결함이 아니라 정상이며, 깊이 결정표가 이미 그것을 처리한다 (B × K2 → L1).
- **한 칸만 간다.** 직접 의존 노드까지다. 전이적으로 끝까지 밀지 않는다 — 밀면
  뿌리에서 실패한 probe 하나가 그 아래 전부를 L3–L4로 끌어올려
  `TOTAL_PAGE_BUDGET`이 즉시 무너진다. 두 칸 아래가 커리큘럼에 걸려 있으면 그
  노드를 직접 묻고, 아니면 `추정`으로 둔다.
- 선행이 여럿이면 그중 가장 낮은 상한을 쓴다 (정의 간선에 한해).
- 같은 선행을 공유할 뿐인 노드끼리는 전파가 없다. 한 가지의 실패는 간선을
  공유하지 않는 가지로 옮겨 가지 않는다.

### 3-2. 양의 상속 (선행이 K2 이상일 때, 의존 쪽으로)

`CLAUDE.md` 「탐색 전략」대로 한다 — 선행이 K2 이상이면 그것에 의존하는 노드들을
K2로 가정하고, 그중 하나만 spot-check한다. **여기에는 한 칸 제한이 없다.** 이것은
그래프가 강제하는 상한이 아니라 질문 예산 절약책이고, spot-check가 안전장치이기
때문이다. spot-check가 실패하면 가정을 취소하고 그 가지의 선행 쪽으로 내려가
어디서 끊기는지 찾는다.

---

## 4. 어긋났을 때 — 의존 노드가 선행보다 높게 나온 경우

흔한 일이고, 대개 사용자의 결함이 아니라 **우리 그래프가 틀렸다는 신호다.**
원인은 셋 중 하나다.

1. 블랙박스 숙달 — 진술을 정확히 쓰지만 근거는 모른다. 깊이 결정표가 이미 처리한다.
2. 정의 간선인 줄 알았는데 실은 증명 간선이었다. 간선 종류를 고친다 (§3).
3. probe가 다른 것을 쟀다. §6의 「등급 근거로 쓰지 않는 답」을 확인한다.

어느 경우든 **직접 증거를 깎지 않는다.** 그래도 그 노드를 얇게 쓰는 것이
불안하면 **K가 아니라 깊이를 한 단계 올린다.** 사유를 적고 승인을 받는 절차가
이미 있다 (`CLAUDE.md` 「깊이 결정표」의 예외). K를 조작하는 것과 달리 이 방법은
흔적이 남고, Phase 4의 재설명 금지 판정을 망가뜨리지 않는다.

---

## 5. 무엇을 묻는가

**먼저 묻는다** (조건을 많이 만족할수록 앞):

1. 논문의 핵심 논증에서 직접 쓰이는 C/D 노드.
2. K등급에 따라 확정 깊이가 크게 달라지는 노드 — 특히 **K1/K2 경계.** 결정표에서
   한 칸이 두 등급을 가르는 곳이다. K2/K3 경계도 어느 태그에서든 한 단계를
   움직이지만, 그 한 단계가 분량을 크게 바꾸는 것은 D 노드다.
3. 여러 중요한 의존 노드의 선행인 노드 (한 번 물어 여러 칸을 채운다).
4. 현재 등급이 가장 불확실한 노드.
5. 선행 쪽에 있어 한 답으로 넓은 범위의 상한을 정할 수 있는 노드.

**묻지 않는다:**

- **A 태그(배경 언급) 노드.** 결정표에서 A는 K1이면 L1, K2면 L0이다 — 답이 바꾸는
  것은 한 문단뿐이고, 그 한 문단에 질문 예산을 쓰지 않는다.
- baseline 항목. 이미 「가정하고 가르치지 않는다」로 확정된 것이다.
- 어떤 답이 나와도 확정 깊이가 같은 노드.
- 이미 직접 증거가 있는 노드. 더 정확히 재려고 후속 질문을 반복하지 않는다.
- 정의 간선의 선행이 이미 낮게 확정되어 상한이 정해진 노드. **정해진 상한을
  다시 확인하려고 묻지 않는다.**

---

## 6. probe 설계와 채점

좋은 probe는 논문이 실제로 요구하는 능력 하나만 재고, 자기보고가 아니라 수행을
요구하며, 노리는 경계를 판정한다.

| 경계 | 무엇을 보는가 |
|---|---|
| K0/K1 | 핵심 정의·정리·표기를 알아보는가. 재인(recognition)이면 충분하다 |
| K1/K2 | **가장 중요한 경계다.** 정리를 기억하는가가 아니라, 논문이 쓰는 전형적 상황에서 실제로 적용하는가 |
| K2/K3 | 결과가 아니라 증명·논증의 핵심 구조를 재구성하는가. 주로 D 노드에서 묻는다 |

- **한 probe는 선행 지식 하나만 요구한다.** 둘 이상이 동시에 걸리면 어느 쪽이
  실패했는지 알 수 없고, 그 답은 어느 노드의 근거도 되지 못한다.
- **위 표의 경계 이름은 우리 것이다. 문항에 적지 않는다.** 「K1/K2 경계를
  봅니다」·「이 노드는 D입니다」는 사용자에게 아무 뜻도 아니다. 밝힐 것이
  있으면 그 답이 교재에서 무엇을 바꾸는지를 평문으로 적는다 —
  「이 답에 따라 이 정리를 진술만 인용할지, 예를 붙여 설명할지가 갈립니다」
  (`CLAUDE.md` § 사용자에게 말하는 법의 대응표를 쓴다).
- 부분정답을 올림하지 않는다. 「쓸 수 있던 것으로 기억한다」는 K1이지 K2가 아니다.
- **미답은 K0이 아니라 증거 없음이다.** 커리큘럼이 걸려 있으면 다음 턴에 형태를
  바꿔 재출제한다 (실제로 부분정답 → 재출제로 K0을 확정한 적이 있다). 걸려 있지
  않으면 `추정`으로 둔다.
- **자기지정.** 사용자가 등급을 직접 지정하면 직접 증거로 쓰고 근거 열에
  `자기지정`이라 적는다. 다만 「안다」는 말은 자기지정이 아니다 (`CLAUDE.md`).

**등급 근거로 쓰지 않는 답:**

- 표기·용어 차이 때문에 생긴 오해.
- 계산 실수뿐이고 핵심 개념은 맞은 경우.
- 한 문항이 여러 선행 지식을 동시에 요구한 경우.
- probe가 예산 밖의 고급 지식을 요구한 경우.
- 영어 표현력 때문에 수학적 내용을 판정할 수 없는 경우 — 한국어로 다시 설명해
  달라고 요청할 수 있다. 영어 문법 오류 자체는 지식 부족이 아니다.

---

## 7. 기록 — `background.md`

**열 구성의 정본은 여기다.** 노드마다 최소한 다음을 적는다.

| ID | 노드 | 사용 방식 | probe | 직접 | 추정 | 확정 K | 깊이 | 근거 | 선정 사유 |
|---|---|---|---|---|---|---|---|---|---|
| N01 | | C | Q1 | K1 | — | K1 | L3 | probe | K1/K2 경계 |
| N02 | | D | Q4 | K2 | K2 | K2 | L3 | probe | 핵심 선행 |
| N03 | | B | — | — | K1 | K1 | L2 | 상속 (N01, 정의) | — |

- **근거 = 확정 K가 어디서 왔는가.** 어휘: `probe` / `자기지정` / `상속` /
  `추정` / `baseline` / `논문 고유` (선행 지식이 아니라 논문 자체의 정의·용어).
  `상속`이면 어느 노드에서 어느 간선으로 왔는지 함께 적는다.
- **선정 사유 = 왜 그 노드를 물었는가.** 어휘: `핵심 선행` / `C·D 적용` /
  `K1/K2 경계` / `K2/K3 경계` / `커리큘럼 영향 큼` / `불확실` / `spot-check`.
  짧은 구면 충분하다. 묻지 않은 노드는 비운다.
- 직접·추정·확정을 한 칸에 합치지 않는다 (§2). **묻지 않은 노드의 직접 열은
  비운다** — 상한을 직접 증거처럼 적어 두면 §4의 어긋남을 영영 못 본다.
- 물은 노드와 묻지 않은 노드를 표 둘로 나눠도 된다 (템플릿이 그렇게 되어 있다).
  그때도 열의 뜻은 같다.

---

## 8. 종료

다음을 만족하면 Phase 1을 끝낸다. **모든 노드의 등급을 알아내는 것이 목표가
아니다.**

- baseline이 확정되었다.
- 커리큘럼이 걸린 K경계가 확인되었다.
- 핵심 C/D 노드에 직접 증거가 있다.
- 나머지는 그래프 추정 또는 보수적 기본값으로 채워졌고, 그 사실이 근거 열에
  적혀 있다.

예산에 도달하면 더 묻지 않는다. 남은 불확실성은 §2의 순서로 처리하고, Phase 2에서
해당 선행 지식을 충분히 설명하는 쪽으로 설계한다.

---

## 금지 사항 (Phase 1 한정)

- **직접 증거를 그래프 추정으로 깎기.** 어긋남은 §4로 처리한다.
- **상한을 전이적으로 전파하기.** 의존 쪽으로 한 칸까지다 (§3-1). 양의 상속은
  이 제한을 받지 않는다 (§3-2).
- 「X를 아세요?」식 자기보고 질문 (`CLAUDE.md`).
- **등급·경계·태그 기호를 문항이나 채점 설명에 그대로 노출하기** (§6,
  `CLAUDE.md` § 사용자에게 말하는 법). 채점 결과를 알릴 때도 「K1로 봅니다」가
  아니라 「진술은 알아보시지만 정확히 쓰지는 못하는 수준으로 봅니다(K1)」.
- 「한 문항만 더 물으면 정확해질 것 같다」는 이유로 예산을 넘기기.
- **자기반성 턴.** 「내가 놓친 선행 지식은 없나」, 「이 답을 보니 다른 것도 물어야
  하나」를 판단하려고 턴을 쓰지 않는다. 매 턴의 질문 후보는 §5의 고정 기준으로만
  만든다.
- 측정값과 추정값을 구분하지 않고 기록하기.
