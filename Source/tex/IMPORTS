# 다른 프로젝트에서 fork해 온 장. `sh tex/import-check.sh`가 이 파일을 읽는다.
#
#   <이 프로젝트의 파일명> | <원본 경로> | <갈라진 시점 원본의 sha256> | <날짜>
#
# 해시를 얻는 법:  shasum -a 256 "<원본 경로>"
# 원본이 바뀌면 검사가 실패한다. 그때 diff를 읽고 판단한다 —
# 자동 병합하지 않는다. 정본은 원본이다 (CLAUDE.md 「교차 프로젝트 재사용」).
