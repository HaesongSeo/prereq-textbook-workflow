한국어 | [English](CLAUDE.en.md)

# CLAUDE.md — 논문 독해용 맞춤 LaTeX 교재 프로젝트

이 파일은 **정책(policy)** 이다. 프로젝트 진행 상태는 절대 여기에 기록하지 않는다.
상태는 `spec.md`, `background.md`, `progress.md`에 기록하며, **매 턴이 끝날 때마다
그 턴에서 바뀐 것을 세 파일에 반영한 뒤 응답을 마친다.** 다음 턴으로 미루지 않는다
(세션이 끊기면 그 턴의 작업은 복구되지 않는다). 반영할 것이 없으면 없다고 확인만 한다.

---

## 0. Project Config

**이 파일은 `Study/` 루트에 있어 모든 프로젝트가 공유한다.** 따라서 Config는
여기가 아니라 **각 프로젝트의 `spec.md` 첫머리**에 적는다. 아래는 항목의
정의이며, 값을 여기에 채우지 않는다.

| 항목 | 뜻 |
|---|---|
| `TARGET_PAPER` | 저자, 제목, 출판 정보 |
| `SOURCE` | arXiv ID / DOI / 로컬 PDF 경로 |
| `OUTPUT_LANGUAGE` | 기본값 **English**. `tex/` 아래 산출물은 영어로 쓰고 pdflatex로 조판한다. 관리 문서(spec/background/progress)와 대화는 한국어를 유지한다 |
| `TOTAL_PAGE_BUDGET` | **선행 챕터(Part I)의 합계 상한.** Phase 5의 정독 분량은 여기 포함되지 않는다 (§ Phase 2 「Part II의 분량」). 예: 140 pages |
| `PER_CHAPTER_MAX` | 한 장의 상한. **선행 챕터와 정독 절 양쪽에 적용된다.** 예: 15 pages |
| `BASELINE_HINT` | 가정할 지식의 하한선. Phase 1에서 probe로 확정한다 |

해당 프로젝트의 `spec.md`에 Config가 비어 있으면 **Phase 0을 시작하지 말고
먼저 질문한다.**

---

## 1. Objective

목표 논문을 이해하는 데 **실제로 필요한 수학만** 가르치는 LaTeX 교재를 만든다.

- 커리큘럼은 논문이 결정한다. 과목이 결정하지 않는다.
- 완전성과 관련성이 충돌하면 **항상 관련성**을 택한다.
- 사용자가 이미 아는 것은 쓰지 않는다. 모르는 것도 논문에 필요한 깊이까지만 쓴다.
- 장기 대화형 프로젝트다. 매 세션이 이전 세션 위에 쌓여야 한다.

---

## 사용자에게 말하는 법 (모든 Phase에 적용)

**등급 기호는 우리의 작업 장치다. 사용자의 것이 아니다.** `K0`–`K3`, `L0`–`L4`,
사용 방식 태그 `A`–`D`, 간선 종류(정의/증명), 노드 ID(`N01`), baseline 항목
번호는 전부 이 파일과 `PHASE1_PROBE_GUIDE.md`, 그리고 관리 문서 안에서만 뜻이
정해진다. 사용자는 그 파일들을 열어 두고 대화하지 않는다. 그래서 대화창에서
기호만 내밀면 아무것도 전달되지 않는다.

> **나쁨:** 「D 노드이므로 K2/K3 경계가 L3와 L1을 가릅니다.」
>
> **좋음:** 「이 정리는 논문이 증명 기법 자체를 흉내 내는 자리입니다. 증명까지
> 재구성하실 수 있으면 진술만 인용하고 지나가고, 진술·적용 수준이면 예와
> 계산까지 싣습니다. 그래서 이 문항의 답이 이 장의 분량을 몇 쪽 바꿉니다.」

규칙은 하나다. **뜻을 평문으로 먼저 쓰고, 기호는 괄호 안 보조 표기로만
붙인다** — 「증명까지 재구성 가능(K3)」처럼. 기호를 없애라는 것이 아니다.
관리 문서에서는 기호가 정본이고, 사용자가 기호로 물으면 기호로 답해도 된다.
금지되는 것은 **기호가 문장의 유일한 근거인 채로 사용자의 판단을 요구하는
것**이다.

특히 걸리는 세 자리:

- probe를 낼 때. 「이 문항은 K1/K2 경계를 봅니다」가 아니라, 그 답이 교재에서
  무엇을 바꾸는지 한 줄로 적는다.
- Phase 종료 요약과 승인 요청. 표 안에는 기호가 있어도 좋다. 다만 승인을 구하는
  문장은 표를 보지 않고도 읽혀야 한다. 기호로만 채운 표를 붙이고 「승인해
  주세요」로 끝내지 않는다.
- 분량 이야기. 깊이는 쪽수로 옮겨 말하는 편이 언제나 낫다. 사용자가 실제로
  판단하는 것은 등급이 아니라 지면이다.

### 대화에서 쓰는 말

| 기호 | 대화에서 |
|---|---|
| K0 | 처음 보는 것 |
| K1 | 진술은 알아보지만 정확히 쓰지는 못하는 수준 |
| K2 | 정확히 진술하고 적용할 수 있는 수준 |
| K3 | 증명까지 재구성할 수 있는 수준 |
| L0 | 싣지 않는다 (필요하면 표기만 맞춰 준다) |
| L1 | 진술과 출처만 |
| L2 | 진술 + 논문에서 쓰이는 방식 |
| L3 | 거기에 예와 계산까지 |
| L4 | 증명까지 전부 |
| A | 배경으로 언급될 뿐 논증에는 쓰이지 않는 것 |
| B | 진술만 갖다 쓰는 것 |
| C | 가설을 확인해 가며 적용하는 것 |
| D | 증명 기법 자체를 흉내 내는 것 |
| 정의 간선 | 그것 없이는 진술 자체를 쓸 수 없는 선행 |
| 증명 간선 | 진술은 읽히지만 증명에 필요한 선행 |

### 이 규칙은 대화창에만 적용된다

**반대 방향의 실수가 더 나쁘다** — 이 규칙을 관리 문서까지 끌고 들어가
기호를 평문으로 풀어 쓰는 것. 자리마다 규칙이 다르고, 셋을 섞지 않는다.

| 자리 | 규칙 |
|---|---|
| 관리 문서 (`spec.md`·`background.md`·`progress.md`·`refs/NOTES.md`) | **기호가 정본이다.** 사용 방식·직접·추정·확정 K·깊이 열에는 `A`–`D`, `K0`–`K3`, `L0`–`L4`를 **그대로** 적는다. 평문 풀이를 병기하지도 않는다 |
| 대화창 | 평문이 먼저, 기호는 괄호 안 (위) |
| 본문 (`tex/`) | 기호를 아예 쓰지 않는다. 괄호에 넣는 것도 안 된다 (§ Phase 4 「챕터 헤더」) |

이유는 취향이 아니다. 깊이 결정표는 조회표이고 「K2 이상 재설명 금지」는 K열을
기계적으로 읽는 판정이라, 등급이 문장으로 적히면 둘 다 성립하지 않는다.
`md-check.sh` ⑧이 검사한다 — `K2 이상` 같은 짧은 단서는 통과하고 평문 풀이를
병기하면 걸린다.

관리 문서에서 평문이 들어갈 자리는 표의 등급 열이 아니라 산문 절이다 —
「진단 총평」, Amendments의 사유 열, 표 아래 주석. 거기서도 기호를 지우지 말고
평문 옆에 남긴다.

---

## 2. 파일 구조와 세션 프로토콜

### 세션 시작 시 (예외 없음)

1. `progress.md`를 읽는다 → **맨 위 상태 블록**에서 현재 Phase와 다음 작업을
   확정하고, 이어서 「현재 상태」의 산문을 읽는다
2. `spec.md`를 읽는다 → 커리큘럼 계획과 amendment 로그 확인
3. `background.md`를 읽는다 → 사용자 지식 상태표 확인
4. 그 다음에야 작업을 시작한다. 이 3개를 읽기 전에 어떤 수학도 쓰지 않는다.

### 상태 블록 (`progress.md` 최상단)

`progress.md`는 **YAML 프런트매터**로 시작한다. 세션 재개가 산문 해석이 아니라
값 읽기가 되게 하는 것이 목적이다.

```yaml
---
phase: 4                       # 0-6
phase_status: in_progress      # in_progress | awaiting_approval | done
last_approved: "2026-08-15 Ch07"
next_action: "Ch08 (V01-V07) 집필"
chapters_approved: [01, 02, 03, 04, 05, 06, 07]
sections_approved: []          # Phase 5의 paper/secNN
---
```

- **이 블록이 `phase`·`last_approved`·승인된 장 목록의 정본이다.** 「현재 상태」의
  산문은 사람이 읽는 요약이며, 블록이 담지 못하는 것(정독 순서, 재설명 금지
  목록, 왜 멈췄는가)을 담는다. **같은 사실을 두 번 적는 것이 아니라, 서로 다른
  것을 적는다.**
- **승인을 받으면 같은 턴에 셋을 함께 갱신한다** — 이 블록, 「승인 로그」,
  「챕터 진행 현황」의 승인 열. `md-check.sh` 항목 ⑦·⑨가 셋의 일치를 검사한다.
- `phase_status`가 `awaiting_approval`이면 **다음 세션은 승인을 먼저 확인한다.**
  승인 없이 다음 Phase의 작업을 시작하지 않는다.

### 디렉터리 레이아웃

```
Study/
  CLAUDE.md          # 이 파일 (정책, 거의 불변, 모든 프로젝트가 공유)
  PHASE1_PROBE_GUIDE.md  # Phase 1 세부 정책 (probe 선정 / K등급 판정). 공유
  README.md
  CLAUDE.en.md           # 위 셋의 영어판. 내용은 같고 언어만 다르다
  PHASE1_PROBE_GUIDE.en.md
  README.en.md
  TRANSLATIONS           # 영어판이 어느 시점의 한국어판을 옮긴 것인가
  translation-check.sh   # 한국어판이 바뀌어 영어판이 낡았는지 검사
  Source/            # 새 프로젝트를 시작할 때 통째로 복사하는 템플릿
  Source-en/         # 같은 템플릿의 영어판. 영어로 진행하면 이쪽을 복사한다
  <프로젝트>/
    spec.md          # Project Specification (Phase 0 산출물) + Project Config
    background.md    # 지식 진단표 (Phase 1 산출물)
    progress.md      # 현재 단계 / 승인 로그 / 분량 실측 / 세션 이력
    md-check.sh      # 관리 문서 검사기
    refs/
      NOTES.md       # 확보 현황의 정본 + 각 PDF를 읽는 법
      *.pdf          # 확보한 원문
    tex/
      .latexmkrc
      check.sh       # 비ASCII 검사 + clean-room 빌드 + 로그 검사
      notation-check.sh
      import-check.sh  # 다른 프로젝트에서 가져온 장의 표류 검출
      IMPORTS          # 그 장들의 출처와 갈라진 시점 해시
      IMPORT-HEADER.txt # 가져온 파일 첫머리에 붙이는 출처 블록의 양식
      <원문>-check.sh  # 원문 정리 번호 대조기 (프로젝트별로 만든다)
      main.tex
      preamble.sty   # 패키지, 정리 환경, 검증 매크로
      notation.sty   # 표기 매크로 (목표 논문 표기를 따름)
      refs.bib
      chapters/ch01-<slug>.tex
      paper/sec01-<slug>.tex     # Phase 5 산출물
      front/preface.tex          # 머리말 (Phase 6 산출물)
      back/appendix-*.tex        # 독자에게 필요한 표 (표기 대응, 원문 교정 목록)
```

**새 프로젝트는 `Source/`를 복사해 시작한다.** 템플릿에 이미 들어 있는 것을
다시 발명하지 않는다 — 세션 시작 절차, 검사 스크립트, 분량 측정법, 스캔본
취급, 확보 현황 양식.

---

## 3. Phase 개요

| Phase | 이름 | 산출물 |
|---|---|---|
| 0 | 논문 확보 및 분석 | `spec.md` |
| 1 | 배경 진단 | `background.md` |
| 2 | 커리큘럼 설계 | `spec.md`의 챕터 계획 확정 |
| 3 | 참고문헌 확보 | `refs/`, `refs.bib` |
| 4 | 교재 집필 | `tex/chapters/` |
| 5 | 논문 정독 | `tex/paper/` |
| 6 | 최종본 정리 | 검증 표시 방침, 전수 점검, 머리말·부록 |

- Phase를 건너뛰지 않는다.
- 각 Phase 절 끝에 **「종료 조건」 체크리스트**가 있다. **전부 만족한 뒤에
  승인을 요청한다.** 하나라도 비어 있으면 그것을 먼저 말하고, 넘어갈지
  사용자에게 묻는다 — 조용히 넘어가지 않는다.
- 각 Phase 종료 시 **(a) 결과 요약, (b) 남은 질문, (c) 승인 요청**을 제시하고
  멈춘다. **셋 다 등급 기호 없이 읽히게 쓴다** (§ 사용자에게 말하는 법).
- 승인은 `progress.md`에 날짜와 함께 기록한다 (§2 「상태 블록」).

---

## Phase 0 — 논문 확보 및 분석

### 0-1. 원문 확보 (필수 선행)

- 논문 **전문**을 확보한다. arXiv / 저널 페이지에서 검색하고 fetch한다.
- 초록·리뷰·기억만으로 분석을 시작하지 않는다. 전문을 못 구하면 사용자에게 PDF를 요청하고 **대기한다.**
- 확보한 원문은 `refs/`에 기록한다.

### 0-2. 분석

논문에서 다음을 추출한다.

- 실제로 사용되는 선행 주제
- 정의 및 표기
- 주요 정리
- 인용된 결과 중 **논문의 논증에 실제로 쓰이는 것만**

### 0-3. 최소 의존성 그래프

- 노드 = 선행 개념/정리. 간선 = "A를 이해하려면 B가 필요".
- 각 노드에 **논문에서의 사용 방식**을 A–D로 태깅한다 (§ 깊이 규칙 참조).
- **각 간선에는 종류를 적는다** — `정의` (B가 A의 *진술 안에* 나온다) / `증명`
  (B는 A의 *증명에만* 쓰인다). Phase 1의 등급 전파가 이 구분 위에서만 성립한다
  (`PHASE1_PROBE_GUIDE.md` §3). 그때 가서 붙이면 그래프를 통째로 다시 읽어야 한다.
- 이 단계에서 숨은 가정·생략된 증명을 전수조사하지 않는다. 해당 절에 도달했을 때 처리한다.

### 0-4. 산출물 — `spec.md`

- 의존성 그래프 (텍스트 또는 TikZ)
- 선행 지식 체크리스트 (노드 목록 + 사용 방식 태그)
- 표기 용어집 (논문 표기 기준)
- 참고문헌 후보
- 제안 챕터 개요

`spec.md`는 승인 후 **amendment 절차 없이 변경하지 않는다.**

### 0-5. 종료 조건

- [ ] 논문 **전문**을 확보했고 `refs/NOTES.md`에 기록했다 (스캔본이면 읽는 법까지)
- [ ] Project Config 여섯 항목이 `spec.md`에 채워졌다
- [ ] 그래프의 **모든 노드에 사용 방식 태그**(A–D)가 붙었다
- [ ] 그래프의 **모든 간선에 종류**(정의/증명)가 적혔다 — 나중에 붙이면 그래프를
      통째로 다시 읽어야 한다
- [ ] 표기 용어집이 **논문 표기 기준**으로 작성되었다
- [ ] 제안 챕터 개요가 있다

---

## Phase 1 — 배경 진단

목표: 각 노드를 `K0/K1/K2/K3`로 판정한다.

**Phase 1을 시작할 때 `PHASE1_PROBE_GUIDE.md`를 읽는다.** 어느 노드를 물을지,
답을 어떻게 등급으로 옮길지, 묻지 않은 노드를 그래프에서 어떻게 채울지는 그
파일이 정본이다. 아래는 그 파일이 전제하는 뼈대다.

### 지식 척도

<!-- SSOT-CANON: k-scale -->

| 등급 | 의미 |
|---|---|
| K0 | 들어본 적 없음 |
| K1 | 진술을 알아보지만 정확히 쓰지는 못함 |
| K2 | 정확히 진술하고 계산·적용할 수 있음 |
| K3 | 증명을 재구성할 수 있음 |

### 질문 규칙

- "X를 아세요?" 같은 **자기보고형 질문 금지.** 대신 **확인 문제(probe)** 를 낸다.
  - 나쁨: "층 코호몰로지를 아시나요?"
  - 좋음: "다음 정리의 가설 중 빠진 것을 고르세요: …" / "이 예에서 $H^1$을 계산하면?" / "이 진술이 거짓인 이유는?"
- probe와 그 밖의 수학적 질문은 영어로 낸다. 서술과 채점 설명은 한국어를 유지한다.
- **대화창의 수식은 조판되지 않는다.** probe에서 $\LaTeX$ 소스를 그대로 쓰면 읽히지
  않으므로, 수식이 두 줄을 넘거나 첨자가 겹치면 평문 기호로 풀어 쓴다
  (`H^1(X, N) -> H^1(X, Omega)` 처럼). 그래도 안 되면 `.tex`로 조판해 PDF로 건넨다.
- 각 probe는 5줄 이내로 답할 수 있어야 한다.
- **문항에 등급 이름을 적지 않는다.** 그 답이 교재에서 무엇을 바꾸는지를
  평문으로 적는다 (§ 사용자에게 말하는 법, `PHASE1_PROBE_GUIDE.md` §6).
- 턴당 최대 5개 질문, Phase 1 전체 최대 6턴.
- 사용자가 "안다"고만 답하면 K2 이상으로 올리지 않는다. probe에 답하거나 등급을 직접 지정하게 한다.

### 탐색 전략 (질문 예산 절약)

방향의 어휘. 간선 「A를 이해하려면 B가 필요」에서 B가 A의 **선행**, A가 B의
**의존**이다. 「상위/하위」는 정반대로 읽히므로 쓰지 않는다.

- **선행 쪽 노드부터** 묻는다. 한 답이 가장 많은 칸을 채우는 자리다.
- 노드가 K2 이상이면 **그것에 의존하는 노드들을 K2로 가정**하되, 그중 1개만
  spot-check한다. spot-check가 실패하면 그 가정을 취소하고, 그 가지의 **선행
  쪽으로 내려가** 어디서 끊기는지 찾는다.
- 노드가 K1 이하이면 그 노드의 **선행으로 내려간다.**
- 즉 그래프 위에서 이진 탐색을 한다. 전수 질문하지 않는다.
- **낮은 등급을 의존 쪽으로 전파하는 규칙, 직접 증거와 추정이 어긋날 때의 처리,
  노드 선정 우선순위와 probe 채점은 `PHASE1_PROBE_GUIDE.md`가 정본이다.** 여기에
  다시 적지 않는다.

### Baseline 확정

- Phase 1 시작 시 **"가르치지 않고 가정할 지식"의 하한선**을 목록으로 제안하고 사용자 확인을 받는다.
- Baseline 아래 항목은 **probe 대상도, 집필 대상도 아니다.** 인용만 한다.
- Baseline은 `background.md` 최상단에 기록한다.

### 산출물 — `background.md`

노드별로 **직접 증거 / 그래프 추정 / 확정 K를 따로 적는다.** 측정값과 추정값을
한 칸에 합치지 않는다 — 이 파일의 K열은 Phase 4의 「K2 이상 재설명 금지」
판정에도 쓰이므로, 어느 쪽이었는지 나중에 알 수 있어야 한다.

**열 구성의 정본은 `PHASE1_PROBE_GUIDE.md` §7이다.** 여기에 다시 적지 않는다.

### 종료 조건

**정본은 `PHASE1_PROBE_GUIDE.md` §8이다.** 여기에 다시 적지 않는다.

---

## Phase 2 — 커리큘럼 설계

### 깊이 등급

| 등급 | 서술 수준 |
|---|---|
| L0 | 제외. 표기 매핑만 (필요 시) |
| L1 | 진술 + 참고문헌 |
| L2 | 진술 + 논문에서 쓰이는 방식 설명 |
| L3 | L2 + 예시·계산 |
| L4 | L3 + 완전한 증명 |

### 논문에서의 사용 방식

| 태그 | 의미 |
|---|---|
| A | 배경 언급만, 논증에 사용 안 됨 |
| B | 블랙박스로 인용 (진술만 갖다 씀) |
| C | 가설을 확인·조작하며 적용 |
| D | 증명 기법 자체를 모방·변형 |

### 깊이 결정표 (사용 방식 × 지식 등급 → 깊이)

<!-- SSOT-CANON: depth-matrix -->

|   | K0 | K1 | K2 | K3 |
|---|---|---|---|---|
| **A** | L1 | L1 | L0 | L0 |
| **B** | L2 | L2 | L1 | L0 |
| **C** | L3 | L3 | L2 | L1 |
| **D** | L4 | L4 | L3 | L1 |

- 이 표를 벗어나려면 **사유를 적고 승인을 받는다.**
- 새 선행 지식이 추가로 필요해지면 그것 역시 이 표를 거친다. 항상 **최소 충분 집합**을 택한다.
- 챕터 계획을 사용자에게 제시할 때는 이 표의 기호가 아니라 「무엇을 어디까지
  쓰는가」와 「몇 쪽인가」로 말한다 (§ 사용자에게 말하는 법). 표는 붙여도
  좋지만, 승인을 구하는 문장 자체가 표 없이 읽혀야 한다.

### 분량 예산

- 챕터별 목표 페이지 수를 `spec.md`에 명시한다. **선행 챕터의** 합계는
  `TOTAL_PAGE_BUDGET`을 넘지 않는다.
- 예산 초과가 예상되면 집필 전에 보고하고 어떤 항목의 깊이를 낮출지 제안한다.
- **`TOTAL_PAGE_BUDGET`은 선행 챕터(Part I)만 덮는다.** Phase 5의 정독 분량은
  예산 대상이 아니다 (아래 「Part II의 분량」).

#### 목표 쪽수를 잡는 법 — **K등급이 아니라 항목 수로 센다**

「이 노드는 K0이니 두껍겠다」로 잡으면 크게 빗나간다. K등급은 분량을 직접
정하지 않는다 — K0이어도 사용 방식이 B면 깊이는 L2이고, L2에서 쓸 수 있는
것은 진술과 역할뿐이다.

**분량을 정하는 것은 노드가 낳는 「항목 수」다.** 항목 = 본문에 번호가 붙는
정의·정리·명제·보조정리·따름정리·예·주석.

**1단계. 노드마다 항목 수를 센다.** Phase 0의 그래프와 원문을 보고 직접 센다.

- 논문이 그 노드에서 실제로 쓰는 진술의 개수 (여기가 바닥이다)
- 그것을 진술하려면 먼저 세워야 하는 정의의 개수
- 깊이 등급이 허락하는 예·계산의 개수

**2단계. 깊이 등급으로 항목 수를 조정한다.** 깊이는 *항목의 길이*가 아니라
*항목의 개수*를 정한다 — 이것이 핵심이고, 틀리기 쉬운 자리다.

| 깊이 | 항목 수에 미치는 영향 |
|---|---|
| L1 | 논문이 쓰는 진술의 수, 그뿐. 예를 더하지 않는다 |
| L2 | 거기에 역할 설명(항목이 아니다). 예는 최대 1개만 더한다 |
| L3 | 항목당 예·계산을 1개까지 더한다. 대략 **L2의 1.5~2배** |
| L4 | **증명은 항목을 늘리지 않는다.** 이미 있는 항목 안으로 들어간다 |

**L4가 길다는 것은 착각이다.** L4가 요구하는 것은 완전한 증명이지 분량이 아니다.

**3단계. K등급으로 보정한다 — 항목 수에, 작게.** K등급은 이미 깊이 결정표를
통해 한 번 반영되었으므로 여기서는 잔여분만 본다.

| K등급 | 보정 |
|---|---|
| K0 | **+2~3항.** 정의와 첫 예를 새로 세워야 한다 |
| K1 | **+1항.** 진술은 알므로 정의는 짧고, 「정확히 쓰지 못하는」 자리에 예 하나 |
| K2 이상 | **0항.** 인용하고 지나간다 (「재설명 금지」) |

**4단계. 쪽수는 항목 수 ÷ 2.5.**

#### Part II (논문 정독)의 분량 — **원문 절 쪽수를 그대로 쓴다**

위의 네 단계는 선행 챕터의 것이다. Phase 5의 절에는 적용하지 않는다 — 그
자리에서 쓸 것은 우리가 고른 항목이 아니라 **논문이 이미 쓴 것**이다.

**절의 목표 쪽수 = 그 절의 원문 쪽수.**

- 이 값은 **참고값이지 예산이 아니다.** `TOTAL_PAGE_BUDGET`은 선행 챕터만
  덮으므로 Part II 합계에는 상한 판정이 없다. 원문 쪽수가 사실상의 상한을
  주므로 별도 예산 없이도 폭주하지 않는다.
- 실측은 목표에서 벗어나며, 그것이 정상이다. 선행 챕터가
  이미 준비를 끝낸 절은 짧아지고, 논문이 압축해 쓴 절은 길어진다.
- 원문 절이 `PER_CHAPTER_MAX`를 넘어도 Phase 2에서 미리 나누지 않는다.
  나눌 자리는 그 절을 읽어 봐야 보인다. 그때 판단해 `sec05a`·`sec05b`로
  나눈다 (Phase 5 「파일 이름의 번호」).
- 목표는 `spec.md`「Phase 5 (논문 정독) 순서와 분량」에, 실측은
  `progress.md`「논문 정독 현황」에 적는다.

#### 목표와 실측은 한 방향으로 어긋난다

공식은 그대로 쓰되, **두 목표가 서로 다른 방향으로 빗나간다는 것**을 알고
시작한다. 무작위 오차가 아니라 목표를 어디서 끌어왔는지에서 오는 구조적 편향이다.

| | 목표의 출처 | 어떻게 읽을 것인가 |
|---|---|---|
| Part I | 우리가 고른 항목 수 | **상한처럼 굴고 실측은 그 아래로 떨어진다** |
| Part II | 원문의 쪽수 | **하한으로 읽는다** |

Part II가 그런 이유는 그 부분의 존재 이유가 원문이 압축한 것을 펴는 데 있기
때문이다 — **원문과 같은 길이가 나왔다면 읽은 것이 아니라 옮겨 적은 것이다.**
Part I에서 목표가 `PER_CHAPTER_MAX`를 살짝 넘는 장이 나와도 그 자체로는 계획을
고칠 이유가 못 된다. 실측이 그 아래로 내려오는 일이 흔하다.

### 종료 조건

- [ ] **모든 노드에 확정 깊이가 있다** (결정표 적용값, 또는 승인된 예외)
- [ ] 결정표를 벗어난 항목이 전부 `spec.md`「결정표 예외」에 **사유와 함께** 있다
- [ ] 챕터별 목표 쪽수를 **항목 수를 세어** 잡았다 (K등급으로 어림하지 않았다)
- [ ] **선행 챕터** 목표 쪽수의 합이 `TOTAL_PAGE_BUDGET` 이내다
- [ ] 각 챕터가 `PER_CHAPTER_MAX` 이내다
- [ ] 예산을 넘겼다면 **어느 항목의 깊이를 낮출지 제안**했다 (집필 전에)
- [ ] 정독 범위의 절마다 **원문 쪽수를 목표로** 적었다 (`spec.md`「Phase 5
      순서와 분량」)

---

## Phase 3 — 참고문헌 확보

### 우선순위

1. 사용자가 제공한 자료
2. 원논문
3. 표준 대학원 교재
4. 신뢰할 수 있는 강의노트
5. 기타 공개 자료

### 규칙

- **기억에 의존하지 말고 검색한다.** arXiv / MathSciNet / 저자 홈페이지를 명시적으로 검색하고 fetch한다.
- 정리 번호를 인용하려면 **실제 출처를 확인한 뒤** 적는다. 확인 없이 번호를 적지 않는다.
- 중요한 참고문헌을 못 구하면 **추측하지 말고** 사용자에게 제공 가능한지 묻는다.
- `refs.bib`에는 실제로 인용된 항목만 넣는다.

### 종료 조건

- [ ] `spec.md`의 참고문헌 후보가 전부 **확보되었거나 미확보로 기록**되었다
- [ ] 확보한 문헌마다 `refs/NOTES.md`에 확보 경로가 있다. **스캔본이면 읽는
      법과 페이지 대응까지** 있다
- [ ] 인용할 정리 번호를 **실물과 대조**했다 (기억으로 적은 번호가 없다)
- [ ] 같은 저자의 복수 논문이 있는 키는 **렌더 결과로 동정을 확정**했다
- [ ] `refs.bib`의 `note` 필드에 작업 기록이 없다 (인쇄된다)
- [ ] 제목 안의 수식·고유명사가 중괄호로 보호되었고, `main.bbl`을 눈으로 읽었다
- [ ] 미확보 문헌은 **대체 가능 여부와 함께** 적혔고, 그 문헌에서 인용하는
      진술이 본문에 있다면 `\UNVERIFIED{}`로 **어느 문헌을 못 봤는지 이름과
      함께** 표시되었다 (§ 검증 규칙의 세 상태)

---

## Phase 4 — 교재 집필

Phase 0–3이 승인된 뒤에만 시작한다. 한 번에 **한 챕터씩** 쓰고 승인을 받는다.

### 챕터 헤더 (본문 첫머리에 표시)

- 목적
- 가정하는 선행 지식 (`background.md`를 **보고 쓰되**, 그 파일을 가리키지는 않는다)
- 논문의 어느 부분을 지원하는가 (§ 번호)
- 무엇을 어디까지 증명하는가

**본문은 관리 문서를 가리키지 않는다.** 깊이 등급(L0–L4), 노드 ID, baseline 항목
번호, Phase 번호, `spec.md`·`background.md`·`progress.md`는 전부 우리의 작업
장치이지 독자의 것이 아니다. 헤더의 마지막 항목도 등급 기호가 아니라 평문으로
쓴다 — 「진술과 역할만 적고 증명하지 않는다」처럼. 등급이 *생략의 근거*로
쓰이던 자리(「L2이므로 여기서 멈춘다」)는 지우지 말고 그 근거를 평문으로
바꿔 적는다 (「진술만 인용하므로 여기서 멈춘다」).

### 서술 원칙

- 대학원 강의노트 문체.
- 동기 → 직관 → 예시 → 왜 필요한가. **단, 깊이 등급이 허용하는 범위 안에서만.**
  - L1: 동기 1–2문장. 예시 없음.
  - L2: 동기 + 논문에서의 역할. 예시 최대 1개.
  - L3: 예시·계산 포함.
  - L4: 증명 포함.
- 불필요한 추상화와 교과서식 완전성을 피한다.
- **표기는 목표 논문을 따른다.** 교재의 편의보다 논문 독해가 우선이다. 표준 표기와 다르면 각주로 대응 관계를 밝힌다.

### 챕터 확정 전 검사

1. 도입한 모든 정의·정리·예시가 **이후에 실제로 쓰이는가?** 아니면 "필요한 직관"으로 명시 정당화되는가? 둘 다 아니면 삭제한다.
2. `background.md`에서 K2 이상으로 판정된 내용을 재설명하고 있지 않은가?
3. 분량이 `PER_CHAPTER_MAX` 이내인가?
4. `latexmk -pdf`가 경고 없이 통과하는가?
5. 모든 정리에 출처가 붙어 있는가? (§ 검증 규칙)

### Phase 종료 조건

위 다섯은 **장마다** 거치는 검사다. Phase 전체를 닫는 것은 따로 있다.

- [ ] `spec.md` 챕터 계획의 **모든 장이 승인**되었고, 승인 로그·진행 현황
      표·프런트매터 셋이 일치한다
- [ ] **실측** 총 분량이 예산 이내다 (`progress.md`「분량 실측표」가 정본)
- [ ] `sh tex/check.sh`와 `sh tex/notation-check.sh`가 통과한다
- [ ] import한 장이 있으면 `tex/IMPORTS`에 등록되었고 `sh tex/import-check.sh`가
      통과한다
- [ ] 계획에 없던 장을 쓰지 않았다 (썼다면 Amendment로 승인되어 있다)

---

## Phase 5 — 논문 정독

절 단위로 진행한다. 각 절마다:

- 그 절의 목적
- 표기 설명
- 새 정의 설명
- 생략된 논증의 복원 (필요한 경우에만)
- 앞서 만든 챕터와의 연결 (`\ref{}`로 실제 연결)

숨은 가정과 생략된 증명은 미리 처리하지 않고, 해당 절에 도달했을 때 처리한다.

### 서술 원칙 — 증명은 `proof`에, 주석은 주석에

논문의 증명은 `proof` 환경으로 옮긴다. `remark` 안에 재구성하지 않는다.
논문에 없어서 우리가 복원한 논증도 `proof` 환경에 쓰고 `\OWNPROOF`를 붙인다 —
논문의 것이냐 우리 것이냐는 환경이 아니라 표시가 구분한다.

`remark`는 셋에만 쓴다.

- 원문이 생략한 연결과 배경 설명
- 표기·가설의 함정, 혼동하기 쉬운 두 진술의 구분
- 미해결이거나 불확실한 것

논증을 `remark`로 싸면 두 가지가 무너진다.

- **독자가 논문의 논증과 우리 해설을 구별할 수 없다.** 주석은 「곁들이는 말」로
  읽히므로, 그 안에 든 증명은 논문이 실제로 한 논증이 아닌 것처럼 보인다.
- `\OWNPROOF`·`\OWNCHECK`가 붙을 자리가 사라진다. 두 표시는 증명에 붙는
  것이고, 증명이 주석 안으로 들어가면 무엇이 우리 것인지 표시할 지점이 없다.

### 파일 이름의 번호는 논문의 절 번호다

`paper/secNN-<slug>.tex`의 `NN`은 **논문의 절 번호**다.
읽는 순서는 `main.tex`의 `\include` 순서와 `spec.md`가 정한다.

정독 순서는 논문 순서와 다른 것이 정상이므로(`spec.md`「Phase 5 (논문 정독)
순서와 분량」) 파일 번호를 읽는 순서로 매기는 관례도 가능하다.
한 절이 길어 `PER_CHAPTER_MAX`에 닿으면 `secNNa`·`secNNb`로 나눈다. 논문에 없는
절(우리가 묶은 도구 모음 등)을 만들지 않는다.

### 종료 조건

- [ ] 정독 범위로 승인된 절이 전부 `tex/paper/`에 있고 승인되었다
- [ ] 각 절이 앞서 만든 챕터를 **`\ref`로 실제 연결**한다 (말로만 가리키지 않는다)
- [ ] 복원한 논증에 `\OWNPROOF`가, 검산·보조사실에 `\OWNCHECK`가 붙었다
- [ ] **원문 정리 번호 대조기**(`tex/<원문>-check.sh`)를 만들었고 통과한다 —
      본문이 텍스트로 적은 번호는 빌드가 아무것도 검사하지 않는다
- [ ] 남은 미해결 사항이 `progress.md`「논문 정독 현황」에 적혔다

---

## 교차 프로젝트 재사용 (import)

한 프로젝트에서 쓴 장을 다른 프로젝트가 가져다 쓸 수 있다. **이것은 복사가
아니라 fork다.** 원본은 자기 책의 다른 장을 `\ref`로 가리키고 자기 목표 논문을
인용하므로, 가져오면서 반드시 손을 봐야 한다.

문제는 손보는 것이 아니라 그 다음이다. 원본이 고쳐져도 가져온 쪽은 영원히
모른다. 그래서 셋을 건다.

1. 파일 첫머리에 출처 블록 (주석이므로 인쇄되지 않는다). 원본 경로, 갈라진
   날짜, 그리고 `ADAPTED:` — **무엇을 손봐야 했는지의 목록.** 마지막 항목이
   핵심이다. 다시 가져올 때의 조리법이 된다.
2. `tex/IMPORTS` — 원본 경로와 **해시 둘**을 기록한다. 갈라진 시점 원본의
   sha256, 그리고 손본 뒤 우리 파일의 sha256.
3. `sh tex/import-check.sh` — 둘을 다시 계산해 **바뀌었으면 실패한다.** 자동
   병합하지 않는다. 원본이 바뀌었으면 diff를 읽고, **우리 쪽이 바뀌었으면
   무엇을 고쳤는지 `ADAPTED:`나 `FIXED:`에 적고 해시를 갱신한다.** 검사기는
   적응과 수학 수정을 구분하지 못하므로 그 판단만은 사람이 한다.

### 정본의 방향

**수학 내용의 정본은 원본이다.**

예외로 가져온 쪽에서 오류를 발견하면 **가져온 쪽만 고친다.** 그 수정은 파일
첫머리의 `FIXED:`에 적는다 — `import-check.sh`는 원본이 바뀌었는지만 보므로,
적어 두지 않으면 두 서술이 갈라진 사실이 어디에도 남지 않는다.
또다른 예외는 **적응**이다 — 표기·인용 키·바깥 참조·챕터 헤더는 두 책이
서로 다른 것이 정상이며, 원본으로 되돌리지 않는다. 그 목록이 `ADAPTED:`다.

### 독자에게는

**본문에 「다른 프로젝트에서 가져왔다」고 적지 않는다** (§ Phase 4). 그것은 우리
작업 이력이다. 다만 두 책이 함께 돌아다닐 수 있다면 머리말에 한 문장으로
공유 사실을 밝힌다 — 독자가 두 개의 독립된 서술로 오해하지 않도록.

---

## Phase 6 — 최종본 정리

Phase 5가 끝나도 책은 아직 초안이다. 남는 것은 세 가지이고, 전부 승인 사항이다.

1. 검증 표시의 최종 방침. 초안에서 눈에 띄게 해 둔 `\OWNPROOF`·`\OWNCHECK`·
   `\uncertain`을 최종본에 남길지 정한다. **남기기로 했다면 책 안에 범례를
   둔다** — 표시가 수십 번 나오는데 뜻을 적은 곳이 없으면 독자는 알 길이 없다.
   결정 전에 `\OWNPROOF`를 전수 분류한다: 출처의 공백을 메운 것과 단순 검산은
   성격이 다르고, 그 구분이 이 책 가치의 소재를 그대로 보여 준다.
2. 전수 점검. `\ref`는 빌드가 잡아 주지만 **텍스트로 적은 원문 정리 번호와
   손으로 쓴 표기는 아무것도 잡아 주지 않는다.** 두 검사기를 만들어 돌린다
   (`progress.md`「검사 스크립트」). 인용 키가 실제로 어느 문헌으로 렌더되는지도
   이때 전수 대조한다.
3. 독자용 앞뒤 장치. 머리말(이 책이 무엇인가 / 두 Part의 관계 / 정독 순서와
   그 이유 / 표시 범례)과, 관리 문서에만 있던 것 중 독자가 필요로 하는 것
   (여러 논문의 표기 대응표 등)을 부록으로 옮긴다.

**본문에서 작업 장치를 걷어내는 것도 여기서 한다** (§ Phase 4 「챕터 헤더」).

### 종료 조건

- [ ] `\OWNPROOF`를 **전수 분류**했고, 검증 표시의 최종 방침이 승인되었다
- [ ] 표시를 남기기로 했다면 **책 안에 범례**가 있다
- [ ] 두 검사기(표기, 원문 번호)의 glob이 **재귀형**이고, 나중에 쓴 머리말·부록이
      그 범위 **안에** 있다. 「0건」이 무엇을 보고 0건인지 확인했다
- [ ] 인용 키 → 문헌 대응을 전수 대조했다
- [ ] 머리말이 있다 (이 책이 무엇인가 / 두 Part의 관계 / 정독 순서와 그 이유 /
      표시 범례)
- [ ] 관리 문서에만 있던 것 중 **독자가 필요로 하는 것**이 부록으로 옮겨졌다
- [ ] `spec.md`「원문 교정 기록」이 비어 있지 않으면 **독자용 부록으로 나갔다.**
      원문과 대조하며 읽는 독자에게는 한 곳에 모인 목록이 필요하다
- [ ] 본문에 깊이 등급·노드 ID·Phase 번호·관리 문서 파일명이 하나도 남아 있지 않다

---

## 검증 규칙 (모든 Phase에 적용)

이 절은 수학 내용의 신뢰성에 관한 것이며, 다른 어떤 규칙보다 우선한다.

- **모든 정리·명제 진술에 정확한 출처를 붙인다.** 예: `[Har77, Thm.~III.5.2]`. 저자·연도만 적는 인용은 불가.
- 존재하지 않는 논문, 정리 번호, 페이지를 생성하지 않는다. 불확실하면 검색하거나 표시한다.
- **인용 키가 어느 문헌으로 렌더되는지 먼저 확정한다.** 같은 저자의 논문이
  여럿이면 저자 이름만 보고 동정하지 말고, 렌더된 원문 PDF의 참고문헌 목록에서
  alpha 라벨 → 문헌 대응을 확인한 뒤 절·정리 번호를 대조한다.
- 출처에 공백이 있어 우리가 채운 논증은 `\OWNPROOF{}`로 표시한다. 출처가 결과만
  진술하거나, 증명을 한 구절로 압축했거나, 다른 진술을 증명한 경우가 여기 해당한다.
  출처 증명의 재구성과 구분한다.
- 검산·예시, 또는 서술 편의로 우리가 진술·증명한 표준 보조사실은 `\OWNCHECK{}`로
  표시한다. 어느 출처의 공백도 메우지 않으므로 `\OWNPROOF`보다 눈에 덜 띄게 둔다.
- 예시·반례의 계산은 검증 가능해야 한다. 검증하지 않은 계산은 표시한다.

네 매크로는 `preamble.sty`에 있다 (초안에서는 눈에 띄게, 최종본 방침은 Phase 6).
쓰다가 조용히 사라지는 함정은 § 조용히 깨지는 것들에 있다.

### 확신이 서지 않을 때 — 세 상태를 가른다

| 상태 | 표시 | 본문에 적는 것 |
|---|---|---|
| 출처를 **특정하지 못했다** — 어느 문헌의 어느 번호인지 모른다 | `\UNVERIFIED{...}` | 무엇을 찾지 못했는지 |
| 출처는 특정했으나 **실물을 확인하지 못했다** | `\UNVERIFIED{...}` | 어느 문헌을 못 봤는지 이름을 적는다 |
| 실물을 봤으나 **우리 서술이 불확실하다** | `\uncertain{...}` | 무엇이 불확실하고 왜인지 |

- 가운데 줄이 핵심이다. **실물을 못 본 것은 「불확실」이 아니라 「미검증」이다** —
  우리 서술의 문제가 아니라 확인하지 못한 사실의 문제이고, 독자가 스스로
  확인할 수 있게 어느 문헌인지를 알려 주어야 한다.
- 가운데 줄은 `refs/NOTES.md`「미확보」와 양방향으로 이어 둔다. 한쪽에만 있으면
  둘 중 하나가 낡은 것이다. 나중에 그 문헌을 구하면 두 곳을 함께 고친다.
- 셋 다 조용히 넘어가지 않는다. **표시 없는 추측이 가장 나쁘다.**

---

## 조용히 깨지는 것들 (조판·도구)

앞 절이 수학의 신뢰성이라면 이 절은 도구의 문제다. 공통점은 **경고도 오류도
나지 않는다**는 것이고, 그래서 아는 것 말고는 막을 방법이 없다.

### 조판

- **인자를 받는 표시 매크로는 반드시 인자와 함께 쓴다.** `\uncertain{...}`처럼
  본문을 감싸는 매크로를 `\OWNCHECK` 같은 무인자 표시처럼 한 줄에 홀로 두면,
  다음 글자 하나를 인자로 삼켜 표시가 조용히 사라진다.
- 정리 환경의 선택 인자 안에서 `\cite[...]{...}`는 **첫 `]`에서 끊긴다.** 출처를
  정리 머리에 붙일 때는 포인터 전체를 중괄호로 감싼다 —
  `\begin{prop}[{\cite[\S2.1]{Key}}]`. 인용 약칭 매크로도 마찬가지다.

### bibtex — 어느 검사기도 잡지 못한다

- `note` 필드는 인쇄된다. `alpha` 스타일은 `note`를 그대로 참고문헌에
  출력하므로, 확보 여부·노드 ID·로컬 경로 같은 작업 기록을 적으면 **최종 PDF에
  실린다.** `note`에는 독자가 인용을 쫓는 데 도움이 되는 것만 둔다 (arXiv
  식별자 등). 확보 현황의 정본은 `refs/NOTES.md`다.
- 제목의 수식이 소문자가 된다. `@article`·`@incollection`·`@misc`의 `title`이
  소문자화되면서 수식을 보호하지 않아 `On $L^2$ extension`이 `On $l^2$
  extension`으로 인쇄된다. 그래서 제목 안의 수식과 고유명사는 중괄호로 감싸고
  (`{$L^2$}`, `{O}hsawa--{T}akegoshi`), **챕터를 확정할 때마다 `main.bbl`을 한
  번 읽는다.** (`@book`의 `title`은 소문자화되지 않는다.)
- `.bib`에는 주석 문자가 없다. 항목 밖의 글은 무시되지만 `@`는 어디에 있든
  항목의 시작으로 읽히므로, 주석에 항목 종류를 `@`와 함께 적으면 bibtex가 그
  자리에서 파싱을 잃는다. 주석에서는 sigil 없이 적는다.

### 검사기가 침묵하는 조건

- **`tex/` 아래 비ASCII 한 글자가 로그 검사를 통째로 침묵시킨다.** `main.log`가
  binary로 분류되어 일반 `grep`이 아무것도 찾지 못한다. 그래서 **`tex/` 아래는
  전부 ASCII**이고, 로그는 `grep -a`로 보며, 확정 전에 반드시
  `sh tex/check.sh`를 돌린다. 임기응변 `grep`으로 대신하지 않는다.
- **검사기는 자기 glob 안의 파일만 본다.** 대상 목록 밖의 파일은 인용·표기·원문
  번호를 아무도 보지 않는다. 그래서 **glob은 재귀형으로 둔다** (`**/*.md`,
  `tex/**/*.tex`) — 새 디렉터리가 생겨도 따라 들어간다. 명시 목록이 필요한 것은
  프로젝트 밖의 공유 정책 파일뿐이다. **검사기가 「0건」이라고 말할 때는 무엇을
  보고 0건인지 확인한다.**

---

## 정본과 사본

같은 표가 두 곳에 있는 일은 정상이다 — `README.md`는 방법을 소개해야 하고
`background.md`는 판정할 때 옆에 두고 봐야 한다. **문제는 한쪽만 고쳐지고
아무도 알아채지 못하는 것이다.**

그래서 사본을 표시한다. HTML 주석이므로 GitHub에서 렌더되지 않는다.

```
<!-- SSOT-CANON: depth-matrix -->     정본 쪽. 바로 아래 표가 그 표다
<!-- SSOT-COPY: depth-matrix -->      사본 쪽. 같은 이름을 쓴다
```

**규칙은 「사본은 정본을 덮을 수 있고, 바꿀 수는 없다」이다.** 사본의 각 칸은
정본의 같은 칸을 **포함**해야 하고, 표의 행·열 수가 같아야 한다. 그래서
`README.md`가 `| **A** 배경 언급만 |`처럼 뜻을 덧붙이는 것은 통과하고, `L1`을
`L2`로 바꾸거나 행을 지우는 것은 걸린다. `md-check.sh` 항목 ⑩이 검사한다.

- 정본은 언제나 `CLAUDE.md` 또는 `PHASE1_PROBE_GUIDE.md`다. 정책 파일이
  아닌 곳에 정본을 두지 않는다.
- 표를 고칠 때는 **정본을 고치고 검사기를 돌린다.** 검사기가 사본을 짚어 준다.
- 새로 표를 베낄 때 표시를 붙이지 않으면 검사되지 않는다. 베끼는 순간에 붙인다.

이것은 **관리 문서의 표에만 적용된다.** 산문 요약은 자연어라 기계로 대조할 수
없으므로 정본을 가리키는 한 줄로 충분하고, 프로젝트 간에 가져온 장의 정본
방향은 § 교차 프로젝트 재사용이 따로 정한다 — 거기서는 사본이 수학 내용을
고칠 수 있다.

---

## 질문 vs. 진행

- **질문한다:** 답에 따라 (a) 항목의 포함 여부, (b) 깊이 등급, (c) baseline이 달라질 때.
- **진행한다:** 그 외의 편집상 결정. 가정을 `[가정]`으로 본문에 명시하고 계속 쓴다. 매 세부 결정마다 멈추지 않는다.

---

## 계획 변경 (Amendment)

읽는 도중 새 선행 지식이 드러나는 것은 **정상**이다. 전면 재승인 대신:

1. `spec.md`의 `## Amendments`에 append: 날짜 / 노드 / 발견 위치 / 사용 방식 태그 / 제안 깊이 / 사유
2. 한 줄 승인만 요청한다.
3. 기존 챕터를 재구성하지 않는다. 필요하면 별도 절을 추가한다.
4. 승인 없이 챕터를 추가·삭제하지 않는다.

---

## 금지 사항

> 항목은 **「무엇을 하지 말라」 + 가리키는 절**까지다. 「왜」는 그 절에만 둔다.

- 논문 전문 없이 Phase 0 진행
- `background.md`에서 K2 이상인 내용 재설명
- Phase 1에서 직접 증거를 그래프 추정으로 깎기 (`PHASE1_PROBE_GUIDE.md` §1·§4)
- `spec.md`에 없는 챕터 집필
- **출처 없는 정리 진술**
- **참고문헌·정리 번호 날조**
- 상태 정보를 이 파일(`CLAUDE.md`)에 기록. Project Config도 각 프로젝트의
  `spec.md`에 적는다 (§0)
- 분량을 두 곳에 적기. 정본은 `progress.md`의 「분량 실측표」 하나뿐이다
- 표를 베끼면서 `SSOT-COPY` 표시를 붙이지 않기 (§ 정본과 사본)
- 승인을 기록하면서 세 자리 중 일부만 갱신하기 (§2 「상태 블록」)
- **승인 없이 계획 변경**
- 등급 기호만으로 사용자에게 질문하거나 승인을 요청하기
  (§ 사용자에게 말하는 법)
- 반대로, 관리 문서의 등급 열을 평문으로 풀어 쓰기 (같은 절, `md-check.sh` ⑧)
- 해결된 항목을 「미해결」 목록에 남겨 두기. 해결되는 즉시 미해결 목록에서
  지운다. 경위를 남길 필요가 있으면 승인 로그·세션 이력·Amendments 같은 *이력*
  절로 옮기고, 미해결 목록에는 **앞으로 할 일이 남은 항목만** 둔다.
  (나중에 다시 미해결로 밝혀지면 그때 다시 올린다.)
- 마크다운 파일(`spec.md`, `background.md`, `progress.md` 등)에서 정의되지 않은
  매크로 사용. 이 파일들은 GitHub·에디터에서 그대로 읽혀야 하므로 수식에는
  표준 LaTeX/MathJax만 쓴다 (`\mathrm`, `\mathbb`, `\mathcal`, `\liminf` 등).
  `\D`, `\C`, `\P`, `\Oc`, `\hyp` 같은 프로젝트 매크로는 `tex/` 안에서만 쓴다.
  마크다운에서 그 매크로를 *언급*할 때는 백틱으로 감싸 코드로 표시한다.
